# Cloudflare DNS for every connection, overriding DHCP-supplied and per-profile DNS.
# Deliberately no switcher: this is the machine's DNS. Deployed by `make dns` (see README).
# Arch Wiki: NetworkManager -> "Setting custom global DNS servers".
[global-dns-domain-*]
servers=1.1.1.1,1.0.0.1,2606:4700:4700::1111,2606:4700:4700::1001
